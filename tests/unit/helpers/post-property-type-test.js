
import { postPropertyType } from 'some-posts/helpers/post-property-type';
import { module, test } from 'qunit';

module('Unit | Helper | post property type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postPropertyType([42]);
  assert.ok(result);
});

